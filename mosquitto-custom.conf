# Custom Mosquitto configuration for external access
# Allow anonymous connections
allow_anonymous true

# Listen on all interfaces (0.0.0.0) on port 1883
listener 1883 0.0.0.0

# Log to console for debugging
log_dest stdout
log_type all
log_timestamp true
